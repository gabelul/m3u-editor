# PyTorch CPU-only must be installed from the PyTorch index
# (Alpine uses musl libc — standard PyTorch wheels require glibc)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
flask>=3.0.0
sentence-transformers>=3.0.0
rapidfuzz>=3.6.0
numpy>=1.26.0
