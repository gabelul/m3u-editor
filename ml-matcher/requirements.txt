# PyTorch CPU-only from the PyTorch index (Alpine uses musl — standard wheels need glibc)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
# HuggingFace transformers for model loading (no scikit-learn dependency unlike sentence-transformers)
transformers>=4.41.0
flask>=3.0.0
rapidfuzz>=3.6.0
numpy>=1.26.0
